# Navi Plugin / Widget for PowerShell Standard

This repository stores the module behind the widget for navi in PowerShell.

## Installation

You can install this module via PowerShell Gallery :

```pwsh
Install-Module -Name navi.plugin
```
